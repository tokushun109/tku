import { Sawarabi_Mincho, Lobster } from 'next/font/google'

export const mainFontFace = Sawarabi_Mincho({
    weight: ['400'],
    subsets: ['latin'],
    display: 'swap',
})

export const labelFontFace = Lobster({
    weight: ['400'],
    subsets: ['latin'],
    display: 'swap',
})
